name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Install uv
        run: |
          pip install uv

      - name: Install dependencies
        run: |
          uv sync --extra build

      - name: Extract version from tag
        id: get_version
        run: |
          $VERSION = "${{ github.ref }}" -replace "refs/tags/v", ""
          echo "VERSION=$VERSION" >> $env:GITHUB_ENV
          echo "Version: $VERSION"

      - name: Run tests
        run: |
          echo "Skipping tests due to memory issues on Windows runner"
          echo "Tests should be run locally before tagging releases"
        # Temporarily disabled due to memory issues
        # uv run pytest tests/ -v || echo "Tests completed with status: $?"

      - name: Build Windows executable with PyInstaller
        run: |
          uv run pyinstaller wareflow-ems.spec --clean --noconfirm

      - name: Rename executable with version
        run: |
          $VERSION = "${{ env.VERSION }}"
          if (Test-Path "dist\wems.exe") {
            Move-Item "dist\wems.exe" "dist\wems-$VERSION.exe"
          }

      - name: Get file size
        id: file_size
        run: |
          $FILE = Get-Item "dist\*.exe"
          $SIZE_MB = [math]::Round($FILE.Length / 1MB, 2)
          echo "SIZE_MB=$SIZE_MB" >> $env:GITHUB_ENV
          echo "File size: $SIZE_MB MB"

      - name: Create checksum
        run: |
          $FILE = Get-Item "dist\*.exe"
          $HASH = (Get-FileHash -Algorithm SHA256 $FILE).Hash
          echo $HASH > "dist\wems-${{ env.VERSION }}.exe.sha256"
          echo "SHA256: $HASH"

      - name: Generate release notes
        id: release_notes
        run: |
          $VERSION = "${{ env.VERSION }}"
          $NOTES = @"
          ## WEMS $VERSION

          ### What's Included
          - Windows executable (.exe) - ${{ env.SIZE_MB }} MB
          - No Python installation required
          - All dependencies bundled

          ### How to Install
          1. Download `wems-$VERSION.exe` below
          2. Run the executable
          3. Application will create database on first launch

          ### Checksum
          SHA256: See `.exe.sha256` file below

          ### System Requirements
          - Windows 10 or later
          - 100 MB free disk space
          - No additional dependencies

          ---
          **Full Changelog**: https://github.com/wareflowx/wems/compare/${{ github.event.release.previous_tag_name }}...v$VERSION
          "@
          $NOTES | Out-File -Encoding UTF8 "release_notes.md"
          echo "Created release_notes.md"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: WEMS ${{ env.VERSION }}
          body_path: release_notes.md
          draft: false
          prerelease: false
          files: |
            dist/*.exe
            dist/*.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: wems-${{ env.VERSION }}-windows
          path: |
            dist/*.exe
            dist/*.sha256
          retention-days: 90
